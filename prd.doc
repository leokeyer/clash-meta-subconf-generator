Role: 你是一位资深前端开发工程师，我现在要做这样一个网站。你对 Clash Meta (Mihomo) 的配置语法非常熟悉。
Task: 请帮我编写一个单文件 HTML 页面，实现“Clash Meta 高级配置生成器”。
核心逻辑要求：
基础输入： 提供一个文本框，用户可以粘贴现有的 YAML 配置。使用 js-yaml 库进行解析。
节点添加： 提供另一个文本框，支持批量粘贴节点链接（ss, vmess, vless, trojan, hysteria2, tuic v5）。你需要编写健壮的正则表达式和解析逻辑，将这些链接转换为 Clash Proxy 对象。
高级模式 A (链式代理)：
自动从解析到的代理列表中提取节点名。
提供两个下拉选择框：一个是“跳板 (B)”，一个是“出口 (A)”。
生成逻辑：在出口节点 A 的配置中添加 dialer-proxy: B 属性。
高级模式 B (多出口监听器)：
提供一个勾选框。开启后，遍历所有节点。
为每个节点生成一个 listeners 条目（类型可选 mixed/http/socks，起始端口由用户设定，如 8000 开始递增）。
这种模式下，生成的配置应包含完整的 dns 配置（fake-ip 模式）。
输出控制： 最终生成干净、格式正确的 YAML，并提供“复制到剪贴板”和“保存为 .yaml 文件”的功能。
UI/UX 要求：
使用极简、现代的 UI 风格（类似 Tailwind 或 Bootstrap 的干净感）。
采用响应式布局，分为“第一步：输入”、“第二步：功能设置”、“第三步：预览导出”。
使用状态提示（如：解析成功、添加了 X 个节点、配置已复制等）。
必须是单文件 HTML，外部库（如 js-yaml）请通过 CDN 引入。
代码质量：
注释清晰，特别是协议解析的部分。
考虑到 Vless 的 Reality 属性、Hysteria2 的认证属性等细节。
请直接开始编写完整代码。